import styled from 'styled-components';
import { fonts } from '../../../styles/vars';

export const StoryTileSt = styled.article`
  border: 1px solid black;
`;

export const TitleSt = styled.h2`
  font-size: ${fonts.lg};
`;

export const AuthorSt = styled.h3`
  font-size: ${fonts.md};
`;
